# Macintosh module ownership map

| Macintosh CODE module | Recovered families | Windows source owner | Confidence / evidence |
| --- | --- | --- | --- |
| `Visos (Generic)` | `CPVGDIBitmap`, `CPVSurface`, `CPVScrollableSurface`, `CPVBackBuffSurface`, `CPVZBuffSurface`, generic `CSurface` lifecycle | `src/Visos/Generic/CSurface.h`, `CPVSurface.cpp`, and `CSurface.cpp`; remaining platform bodies stay in the existing `CGDI.cpp` TU | High: contiguous MacsBug families, x86 object offsets, vbptr/vtordisp topology, backing widths, lifecycle order |
| `Visos (Generic)` allocator/status family | `CArena`, `CRAMArena`, `CMBlock`, `CMRAMBlock`, `CBucket`, `CSmallMemory`, `CString` and status infrastructure | `src/Visos/Generic/Memory.cpp`, `Memory.h`, `VSSTAT.CPP` | High: intact portable family ownership from MacsBug order and Windows behavior; function-level correlation remains partial |
| `Visos (Generic)` resource family | `CMogloadArena`, `CMogDir`, `CMogRes`, typed `CRes*` hierarchy | `src/Visos/Generic/MogLoad.cpp`, `MogPatch.cpp`, `MogConst.cpp`, `MogBst.cpp`, and associated headers | High: contiguous generic resource families, matching archive tags, diagnostics, cache lifecycle, and loader behavior |
| `Visos (Generic)` math family | `CVSMath`, `CVSPoint`, `CVSRange`, `CVSRect`, `CVSSize` | `src/Visos/Generic/VSMath.cpp`, `VSMath.h` | High: self-contained fixed-point/trigonometry and geometry primitive family with matching Macintosh symbols |
| `Visos (Generic)` render queue | `CGDI` | `src/Visos/Generic/CGDI.h`, with the authoritative Windows bodies retained in `src/Visos/Windows/CGDI.cpp` | High: the four-method family maps to the 0x10-byte Windows render queue by exact signature, lifecycle, family order, primitive dispatch, and surface ownership |
| `Visos (Mac Specific)` | Macintosh `CSurface::SetLinePtrs`, `CGDIDevice`, `CGDI` platform hooks | Naming evidence only for Windows counterparts | High for `CSurface::SetLinePtrs`; platform ABI is not transferred |
| Windows platform analogue | Win32 GDI, DIB, DirectDraw, WinG/VGA, invisible-window and debug-window backends, startup entry, shell UI, primary-window hosting, switching, and 240-line mode implementation | `src/Visos/Windows`; `src/Platform/Windows/Entry.cpp`, `ShellUI.cpp`, `Primary.cpp` | High: physical Windows TUs remain intact at their existing CMake/link positions; platform bodies are not claimed as portable Macintosh methods. `Primary.cpp` remains mixed because it also emits shared surface methods. |
| `AI` | game objects, managers, players, enemies, projectiles, groups, pathing, and gameplay-object packet state | `src/AI`; mixed Windows owners retained elsewhere | High for intact TU ownership and the accepted Door, Ice, `CEnemyGroup`, `CMine`, and 36-method `CGameObject` contracts. Mixed `LEVELRUN.CPP`, `LEVELVT.CPP`, `LINKSCF.CPP`, and target-only helpers remain intact and neutral where one Macintosh owner cannot be proved. |
| `Control` | `CGame`, `CGameStatus`, `CLevelLoader`, shared startup options, game lifecycle and control helpers | `src/Control/CGame.cpp`, `CGame.h`, `CGameHelp.cpp`, `CGameVT.cpp`, `Options.cpp`, `Options.h` | High for `CGame`, helper ownership, all 17 `CGameStatus` mappings, and exact `SetGameDefaults`, `DisplayHelp`, `StrCmpI`, and `DoCommandLine` behavior/signatures. Windows graphics-option setup remains in the intact mixed `Options.cpp` TU. |
| `Frontend` | screens, menus, UI; recovered `CEditString` family | `src/Frontend`; `CEditString.cpp` is class-owned, remaining TUs retain their Windows boundaries pending per-file audits | High for `CEditString`; module proven, other class ownership remains function-specific |
| `Map` | map/loading | Existing map/loading files; future module pass | Module name proven; class ownership pending |
| `Network` | network | Existing network files; future module pass | Module name proven; class ownership pending |
| `views/2d` | `C2D` group navigation, cancel-move behavior, and object rendering | Logical family spans `src/views/2d/C2D.cpp`, `src/Control/CGame.cpp`, `src/LEVEL/STATE/LEVELSTAT.CPP`, and `src/Frontend/SCREENVT.CPP`; physical Windows owners remain intact | High for 50 `C2D` methods plus `ViewDataCmp`: exact selection transitions, independently matched sparse object-render dispatch, and distinctive status/sort/depth pipeline behavior; ambiguous split bodies and platform-only helpers remain excluded |
| `UtilMain` | utility/runtime glue | Existing core/utility files; future module pass | Module name proven; class ownership pending |

Each migration moves complete Windows translation units without splitting emitted bodies. Logical Macintosh ownership remains separate where a Windows TU mixes classes or compiler artifacts; VC4 object order and exact-set stability remain authoritative after every move.
