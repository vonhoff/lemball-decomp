#ifndef LEMBALL_LEVELNODEPOINT_H
#define LEMBALL_LEVELNODEPOINT_H

// Level node point. Consolidated from per-TU duplicate definitions (LVNODE.CPP, CMover.cpp).

struct LevelNodePoint {
	int m_anValues[3];
};

#endif
